# Database
DB_HOST=db
DB_PORT=5432
DB_USER=bistroflow
DB_PASSWORD=bistroflow_dev
DB_NAME=bistroflow

# JWT RS256 keys (base64-encoded PEM, or leave empty to auto-generate)
JWT_PRIVATE_KEY=
JWT_PUBLIC_KEY=

# Server
SERVER_PORT=8080
GIN_MODE=debug

# Frontend URL for CORS
FRONTEND_URL=http://localhost:3000

# Next.js
NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1
NEXT_PUBLIC_WS_URL=ws://localhost:8080
